title: Juan's Notes
remote_theme: just-the-docs/just-the-docs

permalink: pretty
color_scheme: catppuccin
highlighter: rouge

# Just-the-Docs math switch
math: mathjax

kramdown:
  # keep $$...$$ / $...$ literally in the HTML so MathJax can render it client-side
  math_engine: nil
  syntax_highlighter_opts:
    block:
      line_numbers: false
